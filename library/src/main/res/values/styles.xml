<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- dialog样式 -->
    <style name="common_dialog_bottom_dialog_custom" parent="android:Theme.Dialog">
        <item name="android:windowIsFloating">true</item>    <!--是否浮在界面上-->
        <item name="android:windowIsTranslucent">true</item> <!--是否半透明-->
        <item name="android:windowNoTitle">false</item>       <!--是否有标题-->
        <item name="android:windowBackground">@android:color/transparent</item> <!--窗口背景色透明-->
        <item name="android:backgroundDimEnabled">true</item> <!--背景是否模糊显示-->
    </style>

    <!-- dialog底部弹出菜单动画 -->
    <style name="common_dialog_bottom_menu_animation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/common_dialog_bottom_menu_enter</item>
        <item name="android:windowExitAnimation">@anim/common_dialog_bottom_menu_exit</item>
    </style>
    <style name="common_dialog_loading" parent="AlertDialog.AppCompat.Light">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>